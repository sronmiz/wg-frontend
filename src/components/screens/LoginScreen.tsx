import React from "react";
import { GoogleButton, MainContainer } from "../common";

export const LoginScreen: React.FC<{}> = () => {
  return (
    <MainContainer>
      <GoogleButton />
    </MainContainer>
  );
};
